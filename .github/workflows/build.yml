name: Build Windows x64

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore CrossFireRouteLab.csproj

      - name: Build
        run: dotnet build CrossFireRouteLab.csproj -c Release --no-restore

      - name: Publish self-contained Windows x64
        run: dotnet publish CrossFireRouteLab.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -o publish

      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package | Out-Null
          Copy-Item publish\CrossFireRouteLab.exe package\CrossFireRouteLab.exe
          Copy-Item README.md package\README.md
          Compress-Archive -Path package\* -DestinationPath CrossFireRouteLab-Windows-x64.zip -Force

      - name: Upload Windows ZIP
        uses: actions/upload-artifact@v4
        with:
          name: CrossFireRouteLab-Windows-x64
          path: CrossFireRouteLab-Windows-x64.zip
          if-no-files-found: error
